# ContextBridge

ContextBridge resolves the current `SelectObjectRay` selection and pointer raycast, then supplies that interaction context to the SceneContext snapshot. It has no code-generation dependency.
