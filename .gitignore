# ---- Secrets / local env ----
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# ---- Rust ----
target/
**/target/
**/*.rs.bk
Cargo.lock.orig

# ---- .NET ----
**/bin/
# ...but Rust `src/bin/` is SOURCE (CLI binaries), not build output — keep it tracked.
!**/src/bin/
!**/src/bin/**
**/obj/
*.dll
*.pdb

# ---- Unity ----
**/Library/
**/Temp/
**/Logs/
**/UserSettings/
**/MemoryCaptures/
**/Build/
**/Builds/
*.apk
*.aab
# Unity's Android/IL2CPP scratch tree (CMake + ninja objects). Regenerated by every
# build and several MB of binary churn — never versioned.
**/.utmp/
# Generated IDE project files (Unity rewrites them on every import).
*.csproj
*.sln
*.unityproj
**/.vsconfig
# Unity build logs written by scripts/build-quest.sh
/build-logs/

# ---- Node ----
**/node_modules/

# ---- Python ----
**/__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/

# ---- Runtime data (never commit) ----
# NOTE: runtime personalization records live under .dcvr-data/personalization/ and
# are covered by the .dcvr-data rules below. Do NOT add a bare `personalization/`
# rule — it would also exclude the authored source crate `crates/personalization/`.
.dcvr-data/
**/.dcvr-data/
.run-logs/
/recordings/
/transcripts/
*.jsonl

# ---- Generated red-team output (regenerate with redteam/corpus_gen.py + run_campaign.py) ----
redteam/results*.json
redteam/corpus.json
redteam/*.log

# ---- Logs / temp ----
*.log
*.tmp
*.temp
*.bak
*.swp
.DS_Store
Thumbs.db

# ---- Local tooling / editor ----
.vscode/
.idea/

# ---- ML generated artifacts (regenerated by train/redteam runs) ----
ml/**/*.npz
ml/**/attacks_discovered.jsonl
ml/**/__pycache__/

# generated paper render intermediate
apps/xr-security-eval/report/*.html
_deliverables/
_original-dreamcodevr-daniel/

# ---- third-party Ubiq RoomServer, fetched locally (not redistributed) ----
vendor/
services/roslyn-analyzer/bin/
services/roslyn-analyzer/obj/
