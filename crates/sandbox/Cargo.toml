[package]
name = "dcvr-sandbox"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Isolated sandbox harness for the malicious-code research mode (Mode D): runner trait, wall-clock-killed process runner, worker with retry + dead-letter."

[dependencies]
async-trait = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
async-trait = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
