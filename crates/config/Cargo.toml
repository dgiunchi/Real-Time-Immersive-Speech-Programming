[package]
name = "dcvr-config"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Environment/default configuration for DreamCodeVR+ (secrets via secrecy, never on disk)."

[dependencies]
secrecy = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
