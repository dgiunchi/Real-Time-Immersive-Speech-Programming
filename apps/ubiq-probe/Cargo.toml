[package]
name = "ubiq-probe"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Connect to a real Ubiq RoomServer, JOIN the room, and dump NID 98/93 frames (verify the protocol against real Ubiq)."

[dependencies]
dcvr-unity-transport = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
